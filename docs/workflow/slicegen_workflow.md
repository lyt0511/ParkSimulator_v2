全局约束：
1 从用户输入的prompt给变量mission_id赋值
2 请把<$mission_id>补全，例如若mission_id = 01，则s<$mission_id>替换为s01
3 请按照step01、02、03、...的顺序执行任务

# step01
1. 判断有没有切换到feat/s<$mission_id>分支，若无则终止执行，提示用户切换到该分支
2. 首先根据plans/prd-0_3-plan-0_2.md 中的切片<$mission_id>生成第一张任务卡Slice<$mission_id>，内容：
## 任务卡Slice <$mission_id> - <slice name>（slice name参考plan中切片<$mission_id>的名称命名） 

### 1. 本卡要交付的用户能力
一句话写清：
- 用户/系统在什么状态下，做了什么动作，会看到什么结果。

示例：
- 用户在菜单选择“正常倒车入库”后，可进入模拟页面并看到车辆与车位。
- 用户在运行中按下控制键后，车辆会在画布中移动。
- 用户点击“结束”后，页面显示成功/失败结果。

### 2. 本卡为什么现在做
- 解决什么核心缺口
- 消灭什么最大风险
- 它是 first playable 的哪一段

### 3. 触达的闭环链路
明确写出本卡必须打通的链路：
- Trigger:
- Input:
- Runtime/State:
- Core Logic:
- Derived Facts / Settlement:
- UI Output:

### 4. 影响的状态机
列出本卡涉及的状态和迁移：
- 当前涉及状态：
- 新增/修改的状态迁移：
- 不在本卡处理的状态：

### 5. 影响的数据流
写清本卡要新增/接通哪些数据：
- 输入数据：
- 运行时状态：
- 派生事实：
- 展示状态：

### 6. 范围（In Scope）
- 本卡明确要完成什么

### 7. 非范围（Out of Scope）
- 本卡明确不做什么

### 8. 实现边界 / 约束
- 不允许修改哪些目录
- 不允许新增哪些依赖
- 是否允许改公开接口
- 是否允许临时 debug hook
- 性能/兼容性底线

### 9. 建议改动模块
- 预计会改哪些文件
- 每个文件承担什么职责
- 哪些层一定要同时动，不能只动一层

### 10. 验收标准（行为式）
只写可观察行为，不写“存在”：
- Given / When / Then
- 至少 3 条：正常、边界、异常

### 11. 测试策略（有预算）
- 单元测试：测哪些纯逻辑
- 状态机测试：测哪些迁移
- 集成 smoke：测哪 1~2 条真实闭环
- 本卡不测什么

### 12. 实施步骤（建议 3~5 步）
- Step 1
- Step 2
- Step 3
- Step 4

### 13. 风险与回滚
- 主要风险
- 回滚边界
- 如何确认没有破坏已有能力

### 14. 交付输出格式（要求模型最后怎么汇报）
完成后必须输出：
- 改动文件列表
- 行为验收结果
- 测试结果
- 剩余风险
- 建议 commit message



不要开始编码，等我确认，确认后把当前任务卡写到docs/mission/prd-0_3-s<$mission_id>.md（要求中文，不能有乱码）